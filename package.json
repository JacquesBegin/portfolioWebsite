{
  "name": "portfoliowebsite",
  "version": "1.0.0",
  "description": "Simple web server to handle http requests/responses.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jacques Begin",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  }
}
